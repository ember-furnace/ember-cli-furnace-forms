
{{#each message in errors}}
	<message class="{{message.type}} {{if message.visible 'visible' 'hidden'}}">{{i18n message attributes=message.attributes}}</message>
{{/each}}
{{#each message in warnings}}
	<message class="{{message.type}} {{if message.visible 'visible' 'hidden'}}">{{i18n message attributes=message.attributes}}</message>
{{/each}}
{{#each message in notices}}
	<message class="{{message.type}} {{if message.visible 'visible' 'hidden'}}">{{i18n message attributes=message.attributes}}</message>
{{/each}}

