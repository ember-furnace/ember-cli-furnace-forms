<label  {{bind-attr for=inputId}}>{{caption}}</label>
{{view "select"
       content=options
       optionValuePath="content.index"
       optionLabelPath="content.caption"
       value=selectedIndex
       prompt=placeholder}}
{{#if showControl}}
	{{f-control control dynamic=true}}
{{/if}}
{{f-messages}}