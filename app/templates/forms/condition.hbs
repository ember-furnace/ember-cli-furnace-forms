{{#if _condition}}
	{{#if caption}}
		<h3 class="panel-caption">{{caption}}</h3>
	{{/if}}
	
	{{#each control in inputControls}}
	{{f-control control}}
	{{/each}}
	
	{{#each control in actionControls}}
	{{f-control control}}
	{{/each}}
	{{f-messages}}
{{/if}}
